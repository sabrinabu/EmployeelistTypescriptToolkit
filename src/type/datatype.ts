export type data = {
  id: number| string;
  imageUrl: string;
  firstName: string;
  lastName: string;
  email: string;
  contactNumber:number| string;
  age: number| string;
  dob: string | number;
  salary:number| string;
  address: string;
};
